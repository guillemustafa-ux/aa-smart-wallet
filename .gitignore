# Foundry
cache/
out/
broadcast/
# lib/ NO se ignora: forge la maneja como submódulos de git

# Node / frontend
node_modules/
dist/
.vercel/

# Secretos — NUNCA subir
.env
.env.*
!.env.example

# Orquestación interna (el repo es público) — NUNCA subir
ARRANQUE-FABLE.md
